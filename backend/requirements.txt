# Backend dependencies
# Install with: pip install -r backend/requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Gemini SDK (Google AI Studio — free tier)
google-genai>=1.0.0

# HTTP client for Google Custom Search + GitHub APIs
httpx>=0.27.0

# Environment variable loading
python-dotenv>=1.0.0

# Rich terminal output (used during pipeline runs)
rich>=13.7.0

# Image handling (resize before sending to Gemini)
Pillow>=10.0.0

# Data validation for API request/response models
pydantic>=2.7.0
